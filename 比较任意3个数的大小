#define _CRT_SECURE_NO_WARNINGS 1
#include<stdio.h>
int main()
{
	int x = 0;
	int y = 0;
	int z = 0;
	
	scanf("%d%d%d", &x, &y, &z);
	if (x > y)
	{
		int c = y;
		y = x;
		x = c;
	}
	if (x > z)
	{
		int c = z;
		z = x;
		x = c;
	}


	printf("%d %d %d\n", x, y, z);
	return 0;
