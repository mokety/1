#include<stdio.h>
int main()
{
	int arr[] = { 1,2,3,4,5,1,2,3,4 };
	int s = sizeof(arr) / sizeof(arr[0]);
	int i = 0;
	for (i = 0; i < s; i++)
	{
		int j = 0;
		int count = 0;
		for (j - 0; j < s; j++)
		{
			if (arr[i] == arr[j])
			{
				count++;
			}

		}
		if (count == 1)
		{
			printf("%d\n", arr[i]);
			break;
}

	}



	return 0;
}
